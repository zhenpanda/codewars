// Perform the secret knock() to enter...

let str = knock.toString()
console.log(str)

function (arg) {
 if (arg && arg === knock) {
    console.log('"Knock knock."');
    console.log('Whos\'s there?');
    return deliverLine;
  }
}

let deliverLine = knock(knock).toString();
console.log(deliverLine)

function () {
 if (lineCount() === 2) {
    openDoor();
  }
  return deliverLine;
}

// "Knock knock."
// Whos's there?
// "Harry."
// Harry who?
// "Harry up and open the door, it's cold out here!"
// Groan. That's the worst "knock knock" joke I've ever heard.
// Oh well, I suppose you'd better come in.
// Welcome to the annual meeting of the Knock Knock Joke Society.

success = true;

knock(this[success] = true);
